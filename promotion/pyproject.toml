[project]
name = "promotion"
version = "0.1.0"
description = "Champion/challenger model promotion for vroom-forecast"
requires-python = ">=3.12"
dependencies = [
    "mlflow>=2.21",
    "redis>=4.2,<5",
]

[dependency-groups]
dev = ["pytest>=9.0.3"]

[tool.pytest.ini_options]
pythonpath = ["."]
