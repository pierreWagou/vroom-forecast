procs:
  infra:
    cmd: ["docker", "compose", "--ansi", "always", "up", "--build", "--menu=false"]
    stop: "SIGINT"
  jupyter:
    cmd: ["uv", "run", "--project", "exploration", "jupyter", "notebook", "--no-browser", "--port=8888"]
